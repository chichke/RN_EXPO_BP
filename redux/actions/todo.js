import { FETCH } from '../types/todo';

export const addTodo = () => (dispatch) => {
  dispatch({ type: FETCH });
  // TODO Add Logic
};

export const remTodo = () => (dispatch) => {
  dispatch({ type: FETCH });
  // TODO Remove Logic
};

export const editTodo = () => (dispatch) => {
  dispatch({ type: FETCH });
  // TODO Edit Logic
};
